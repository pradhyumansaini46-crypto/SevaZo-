services:
  postgres:
    image: postgres:16-alpine
    container_name: sevazo-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: sevazo
      POSTGRES_PASSWORD: sevazopassword
      POSTGRES_DB: sevazo_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U sevazo -d sevazo_db"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: sevazo-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
  redis_data:
